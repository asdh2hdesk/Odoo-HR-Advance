<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_upload_attendance_wizard_form" model="ir.ui.view">
        <field name="name">hr.leave.upload.attendance.wizard.form</field>
        <field name="model">hr.leave.upload.attendance.wizard</field>
        <field name="arch" type="xml">
            <form string="Upload Attendance Report">
                <sheet>
                    <group>
                        <group>
                            <field name="file" filename="filename" widget="binary"/>
                            <field name="filename" invisible="1"/>
                        </group>
                    </group>
                    <group>
                        <div class="alert alert-info" role="alert">
                            <h4>Instructions:</h4>
                            <ul>
                                <li>Upload an Excel file (.xlsx) with attendance data</li>
                                <li>The file should have a "Detailed" sheet with employee codes and daily status</li>
                                <li>Status codes: P (Present), A (Absent), W/WO (Week Off), H (Holiday), CL, EL, SL, ML, UL, HD, L</li>
                                <li>The system will skip duplicates, create attendance records for P, and leave records for leave codes</li>
                                <li>Check server logs for detailed processing information</li>
                            </ul>
                        </div>
                    </group>
                </sheet>
                <footer>
                    <button name="action_upload"
                            string="Process Upload"
                            type="object"
                            class="btn-primary"/>
                    <button string="Cancel"
                            class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_upload_attendance_wizard" model="ir.actions.act_window">
        <field name="name">Upload Attendance Report</field>
        <field name="res_model">hr.leave.upload.attendance.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{}</field>
    </record>
</odoo>